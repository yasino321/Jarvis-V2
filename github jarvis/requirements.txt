# ============================================================
#  J.A.R.V.I.S — Python Abhängigkeiten
#  Installation: pip install -r requirements.txt
# ============================================================

# ── PFLICHT (ohne diese startet JARVIS nicht) ─────────────────
python-dotenv>=1.0.0
pygame>=2.5.0
edge-tts>=6.1.9
SpeechRecognition>=3.10.0
pyTelegramBotAPI>=4.14.0
google-genai>=0.8.0
requests>=2.31.0

# ── KI-BACKENDS ───────────────────────────────────────────────
# Groq (Kritiker-Brain + Whisper STT)
groq>=0.9.0
# Cerebras (schnelle Code-Generierung im Optimizer)
cerebras-cloud-sdk>=1.0.0

# ── COMPUTER-CONTROL ──────────────────────────────────────────
pyautogui>=0.9.54
pyperclip>=1.8.2

# ── VISION / SCREENSHOTS ──────────────────────────────────────
opencv-python>=4.8.0
Pillow>=10.0.0

# ── GOOGLE SERVICES (Kalender, Gmail, Tasks) ──────────────────
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.97.0

# ── SYSTEM-MONITORING ─────────────────────────────────────────
psutil>=5.9.0
